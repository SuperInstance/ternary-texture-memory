[package]
description = "ternary-texture-memory - SuperInstance ecosystem crate"
repository = "https://github.com/SuperInstance/ternary-texture-memory"
name = "ternary-texture-memory"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
